# For local development, leave DATABASE_URL unset to use SQLite.
# In production, Railway sets this automatically.
# DATABASE_URL=postgresql://user:password@host:5432/dbname
APP_ENV=development
PORT=8000
