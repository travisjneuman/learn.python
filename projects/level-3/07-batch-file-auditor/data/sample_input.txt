This project audits directories — no input file needed.

Usage:
    python project.py audit .
    python project.py audit . --pattern "*.py"
    python project.py audit . --max-size 500000 --json
    python project.py scan .

Point it at any directory to check for:
- Empty files (0 bytes)
- Large files (over configurable threshold)
- Naming convention violations (spaces, special characters)
- Files without extensions
